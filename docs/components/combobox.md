# 🧩 Combobox

**Sorgente:** `src/components/ui/combobox.tsx`
**Storybook:** [▶ Vedi la Live Demo](https://csa-italy.github.io/havas-ui/?path=/story/ui-combobox--docs)

> ⚠️ Descrizione non disponibile. Aggiungere JSDoc @description al componente.

---

## 💻 Usage

```tsx
import { Combobox } from '@csa-italy/havas-ui'

<Combobox />
```

## ⚙️ Props API

> Nessuna prop documentata per questo componente.

---

## 🧱 Sub-Components

### `ComboboxContent`

> ⚠️ Descrizione non disponibile per ComboboxContent.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{
  className,
  side = "bottom",
  sideOffset = 6,
  align = "start",
  alignOffset = 0,
  anchor,
  ...props
}` | `ComboboxPopupProps & Pick<ComboboxPositionerProps, "align" | "side" | "sideOffset" | "alignOffset" | "anchor">` | `-` | ✅ | — |
### `ComboboxItem`

> ⚠️ Descrizione non disponibile per ComboboxItem.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{
  className,
  children,
  ...props
}` | `ComboboxItemProps` | `-` | ✅ | — |
### `ComboboxGroup`

> ⚠️ Descrizione non disponibile per ComboboxGroup.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{ className, ...props }` | `ComboboxGroupProps` | `-` | ✅ | — |
### `ComboboxLabel`

> ⚠️ Descrizione non disponibile per ComboboxLabel.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{
  className,
  ...props
}` | `ComboboxGroupLabelProps` | `-` | ✅ | — |
### `ComboboxSeparator`

> ⚠️ Descrizione non disponibile per ComboboxSeparator.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{
  className,
  ...props
}` | `SeparatorProps` | `-` | ✅ | — |
### `ComboboxTrigger`

> ⚠️ Descrizione non disponibile per ComboboxTrigger.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{
  className,
  children,
  ...props
}` | `ComboboxTriggerProps` | `-` | ✅ | — |
### `ComboboxValue`

> ⚠️ Descrizione non disponibile per ComboboxValue.

## ⚙️ Props API

| Property | Type | Default | Required | Description |
|----------|------|---------|----------|-------------|
| `{ ...props }` | `ComboboxValueProps` | `-` | ✅ | — |

---
*Generato automaticamente da `scripts/docs/index.mjs` — non modificare manualmente.*
*Ultimo aggiornamento: 2026-04-16T17:16:14.744Z*